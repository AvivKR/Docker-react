language: generic

sudo: required
services:
  - docker

before_install:
  - docker build -t firstproj -f Dockerfile.dev .

script:
  - docker run -e CI=true firstproj npm run test -- --coverage
